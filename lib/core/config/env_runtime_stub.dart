// Stub: used when not compiling for web (no dart:html). Returns null so compile-time config is used.

/// Returns null on non-web platforms; API base URL comes from EnvConfig (dart-define) only.
String? getRuntimeApiBaseUrl() => null;
